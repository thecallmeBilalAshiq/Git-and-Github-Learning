=================================================================================================== 
==================================                            =====================================
==================================         Git-and-Github     =====================================
==================================     Muhammad Bilal Ashiq   =====================================
==================================                            =====================================
===================================================================================================




---------------------------------------------------------------------------------------------
git config --global user.name "thecallmeBilalAshiq"
git config --global user.email "bashiq031@gmail.com"
git config --list 
---------------------------------------------------------------------------------------------
⭐Clone  --> github repository to vs code
git clone https://github.com/thecallmeBilalAshiq/Git-and-Github.git 
cd Git-and-Github
ls 
ls -a  (To see all the files, also hidden ones )

-----------------------------------------------------------------------------------------------------------
⭕ status --> github repository to vs code
to check the files (commit, track, modified, etc)

<!-- Add(engagement stage) and commit(wedding stage) --> 
 git add
⭕ git add [file]
add a file as it looks now to your next commit (stage)
⭕ git reset [file]
unstage a file while retaining the changes in working directory
⭕ git diff
diff of what is changed but not staged
⭕ git diff --staged
diff of what is staged but not yet commited
⭕ git commit -m “[descriptive message]”
commit your staged content as a new commit snapshot

⭕ git log ---> to see all commits
--------------------------------------------------------------------------------------------------------------------------
1. Untracked  --- we add new files, but no commit it,, it is untracked, either we write in it or not
2. modified   --- changed
3. staged     --- file is redy to be committed
4. unmodified --- unchanged
--------------------------------------------------------------------------------------------------------------------------


git remote add origin https://github.com/thecallmeBilalAshiq/LocalRepo.git
git remote -v
git branch
git branch -m main  (rename the branch)
git push origin main
====> instead of it you can use
git push -u origin main
====> for future, now use this short command only
git push
only



------------------------------------------------------------------------------------------------
                                          Branches

git checkout -b newbranch ==> to create new branch
git checkout newbranch (for shifting to different branches)
git checkout -b bilalbranch  
git branch -d bilalbranch      ==> delete

|||||||||||==========>>> Merge two branches <<<================||||||||||||||||
---> via command
git diff main              ===> to check the difference between two branches
git merge main         ===> to merge new branch with the main branch

---> via github
PR (Pull Request)  
after merging on github---> 
git pull origin main
to update it on your computer.

--------------------------------------------------------------------------------------------------------------------------
git reset
git reset HEAD~1   ==> to undo last commit

git reset

Fork ====> To get other people repositories
====> Search their repository,, fork it,,, complete,,, now that person repository is your repository



--------------------------------------------------------------------------------------------
|  Certificate:
|  https://coursera.org/verify/DZ8JGT5UQ7Y3
--------------------------------------------------------------------------------------------
